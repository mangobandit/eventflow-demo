{
  "name": "mxc-wedding-house",
  "private": true,
  "version": "1.1.0",
  "description": "Matt and Cara wedding guest guide, private planner and secure RSVP control centre.",
  "scripts": {
    "check:js": "node --check guest.js && node --check rsvp.js && node --check planner-core.js && node --check planner-auth.js && node --check planner-views.js && node --check planner-forms.js && node --check planner-rsvp.js && node --check sw.js",
    "test:contracts": "node tests/rsvp-contract.mjs",
    "test": "npm run check:js && npm run test:contracts"
  }
}
